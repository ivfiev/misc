"""
This type stub file was generated by pyright.
"""

from collections.abc import Callable
from typing import Any, Union
from sympy import Expr
from torch._inductor.ir import ComputedBuffer, InputBuffer
from ..utils import try_import_cutlass
from cutlass_cppgen.backend.evt.ir.tensor import Tensor as CutlassTensor
from cutlass_library import DataType, EpilogueScheduleType, TileDescription

EpilogueFunctor = Any
Buffer = Union[ComputedBuffer, InputBuffer]
CutlassTupleType = Any
CutlassVisitorType = Any
CutlassArgType = Any
if try_import_cutlass():
    _CUTLASS_C_DTYPES = ...
    class EVTArgRenames:
        """Handles mapping buffer names to variable names in the cpp kernel signature and body"""
        def __init__(self) -> None:
            ...
        
        def new_name(self, name: str) -> str:
            ...
        
        def get(self, name: str) -> str:
            ...
        
    
    
    def create_example_tensors(var_name_to_buffer_name: dict[str, str], name_to_buffer: dict[str, Buffer], size_hint_fn: Callable[[Union[Expr, int]], int]) -> dict[str, CutlassTensor]:
        ...
    
    def trace(fn_src: str, example_tensors: dict[str, CutlassTensor], accum_type: DataType, output_type: DataType, tile_description: TileDescription, epilogue_schedule: EpilogueScheduleType, name_to_buffer: dict[str, Buffer], size_hint_fn: Callable[[Union[Expr, int]], int], **kwargs: dict[str, Any]) -> tuple[str, str, str, EVTArgRenames]:
        ...
    
