"""
This type stub file was generated by pyright.
"""

import logging
import torch
from collections.abc import Callable
from typing import Any, Union

logger: logging.Logger = ...
def is_complex_expr(expr: Any) -> bool:
    ...

class HopPartitionedGraph:
    def __init__(self, fw_gm: torch.fx.GraphModule, bw_gm: torch.fx.GraphModule, n_fw_outputs: int, n_intermediates: int, no_complex_exprs_at_boundary: bool) -> None:
        ...
    


class HopJointGraph:
    def __init__(self, joint_gm: torch.fx.GraphModule, n_primals: int, n_fw_outputs: int, *, functionalized: bool) -> None:
        ...
    
    def partition(self, partition_fn: Callable, always_recompute_complex_exprs: bool) -> HopPartitionedGraph:
        ...
    


def create_hop_joint_graph(fw_fn: Callable, fw_args: tuple[Union[torch.Tensor, torch.SymInt], ...], functionalize: bool) -> HopJointGraph:
    ...

class HopGraphMinCutPartitioner:
    @staticmethod
    def create_partitioned_graph(fw_fn: Callable, fw_args: tuple[Union[torch.Tensor, torch.SymInt], ...], *, always_recompute_complex_exprs: bool = ...) -> HopPartitionedGraph:
        """
        Inputs:
            - fw_fn: the forward function that we'll use to create a joint graph and partition
            - fw_args: the flat_args to fw_fn
            - always_recompute_complex_exprs: when set to True, the bw_gm will do a re-compute
              for inputs that are complex expressions such that the partitioning boundary
              only consists of basic symbols and tensors.

        Returns a HopPartitionedGraph
        """
        ...
    


